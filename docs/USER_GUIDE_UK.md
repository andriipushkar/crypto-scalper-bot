# Керівництво Користувача (Українська)

Повний посібник з використання Crypto Scalper Bot.

## Зміст

1. [Початок роботи](#початок-роботи)
2. [Налаштування](#налаштування)
3. [Веб-панель керування](#веб-панель-керування)
4. [Telegram бот](#telegram-бот)
5. [Торгові стратегії](#торгові-стратегії)
6. [Управління ризиками](#управління-ризиками)
7. [Signal Provider](#signal-provider)
8. [Smart DCA з AI/ML](#smart-dca-з-aiml)
9. [Liquidation Heatmap](#liquidation-heatmap)
10. [News/Event Trading](#newsevent-trading)
11. [Advanced Alerts](#advanced-alerts)
12. [Паперова торгівля](#паперова-торгівля)
13. [Бектестинг](#бектестинг)
14. [Мульти-біржова торгівля](#мульти-біржова-торгівля)
15. [Сповіщення та алерти](#сповіщення-та-алерти)
16. [API Документація](#api-документація)
17. [FAQ](#faq)

---

## Початок роботи

### Швидкий старт (5 хвилин)

1. **Встановлення бота:**
   ```bash
   git clone <repo-url>
   cd crypto-scalper-bot
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   # або venv\Scripts\activate  # Windows
   pip install -r requirements.txt
   ```

2. **Налаштування API ключів:**
   ```bash
   cp .env.example .env
   # Відредагуйте .env з вашими ключами біржі
   ```

3. **Запуск у режимі паперової торгівлі:**
   ```bash
   python -m src.main --paper
   ```

4. **Відкриття панелі керування:**
   ```bash
   python -m src.web.app
   # Відкрийте http://localhost:8000 у браузері
   ```

### Перша торгова сесія

Перед реальною торгівлею рекомендуємо:

1. **Торгувати на папері мінімум 1 тиждень** для розуміння поведінки бота
2. **Переглянути всі стратегії** та їх параметри
3. **Встановити консервативні ліміти ризику** (макс. 1% на угоду)
4. **Почати з одного символу** (наприклад, BTCUSDT)
5. **Уважно моніторити** перші кілька днів

---

## Налаштування

### Змінні середовища (.env)

```bash
# =============================================================================
# Binance API Keys (Основна біржа)
# =============================================================================
# Отримати: https://testnet.binancefuture.com/ (тестнет)
# Продакшн: https://www.binance.com/en/my/settings/api-management

BINANCE_TESTNET_API_KEY=your_testnet_api_key
BINANCE_TESTNET_API_SECRET=your_testnet_api_secret
BINANCE_API_KEY=your_production_api_key
BINANCE_API_SECRET=your_production_api_secret

# Середовище: testnet або production
ENVIRONMENT=testnet

# =============================================================================
# Безпека
# =============================================================================
MASTER_PASSWORD=your_strong_password
JWT_SECRET_KEY=your_jwt_secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_password

# =============================================================================
# Telegram (рекомендовано)
# =============================================================================
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
TELEGRAM_ENABLED=true
TELEGRAM_ALLOWED_USERS=123456,789012  # ID користувачів
TELEGRAM_ADMIN_USERS=123456  # ID адмінів

# =============================================================================
# Додаткові біржі (опціонально)
# =============================================================================
BYBIT_API_KEY=
BYBIT_API_SECRET=
OKX_API_KEY=
OKX_API_SECRET=
OKX_PASSPHRASE=
KRAKEN_API_KEY=
KRAKEN_API_SECRET=
KUCOIN_API_KEY=
KUCOIN_API_SECRET=
KUCOIN_PASSPHRASE=
GATEIO_API_KEY=
GATEIO_API_SECRET=
```

### Конфігурація торгівлі (config/settings.yaml)

```yaml
# =============================================================================
# ЗАГАЛЬНІ НАЛАШТУВАННЯ
# =============================================================================
trading:
  symbols:
    - BTCUSDT
    - ETHUSDT
  leverage: 10
  paper_trading: true
  default_exchange: binance

# =============================================================================
# РИЗИК-МЕНЕДЖМЕНТ (ВАЖЛИВО!)
# =============================================================================
risk:
  max_position_usd: 30        # Макс. розмір позиції в USD
  max_daily_loss_usd: 5       # Денний ліміт втрат
  max_trades_per_day: 20      # Макс. угод на день
  max_open_positions: 1       # Макс. одночасних позицій
  default_stop_loss_pct: 0.5  # Стоп-лосс за замовчуванням (%)
  default_take_profit_pct: 0.3  # Тейк-профіт (%)
  risk_per_trade_pct: 1.0     # Ризик на угоду (%)

# =============================================================================
# СТРАТЕГІЇ
# =============================================================================
strategies:
  orderbook_imbalance:
    enabled: true
    imbalance_threshold: 1.5
    levels: 5

  volume_spike:
    enabled: true
    volume_multiplier: 3.0
    lookback_seconds: 60

  mean_reversion:
    enabled: false
    lookback_period: 20
    std_dev_multiplier: 2.0

  grid_trading:
    enabled: false
    grid_levels: 10
    range_percent: 0.05

  dca:
    enabled: false
    mode: hybrid
    interval_minutes: 60
```

---

## Веб-панель керування

### Доступ до панелі

```bash
# Запуск веб-сервера
python -m src.web.app

# Відкрийте у браузері
http://localhost:8000
```

### Вкладка "Dashboard" (Головна)

Основна інформація в реальному часі:

- **Total P&L** - загальний прибуток/збиток
- **Daily P&L** - денний P&L
- **Win Rate** - відсоток прибуткових угод
- **Trades** - кількість угод
- **Profit Factor** - коефіцієнт прибутковості
- **Max Drawdown** - максимальна просадка

**Equity Curve** - графік еквіті в реальному часі

**Open Positions** - відкриті позиції з кнопкою "Close All"

**Recent Signals** - останні торгові сигнали

**Recent Trades** - історія угод

### Вкладка "Trading" (Торгівля)

#### Розміщення ордерів

```
Форма "Place Order":
- Symbol: вибір торгової пари (BTCUSDT, ETHUSDT, SOLUSDT)
- Side: BUY (купівля) / SELL (продаж)
- Order Type: Market (ринковий) / Limit (лімітний)
- Quantity: обсяг
- Price: ціна (для лімітних ордерів)
- Stop Loss: стоп-лосс (опціонально)
- Take Profit: тейк-профіт (опціонально)
- Leverage: плече (1-50x)
```

#### Налаштування SL/TP

```
Форма "Set SL/TP":
- Symbol: символ позиції
- Stop Loss: ціна стоп-лосс
- Take Profit: ціна тейк-профіт
```

#### Налаштування плеча

```
Форма "Leverage":
- Symbol: символ
- Leverage: плече (1-125x)
```

### Вкладка "Strategies" (Стратегії)

Відображає всі доступні стратегії з можливістю:
- Увімкнення/вимкнення стратегії одним кліком
- Перегляд поточних параметрів
- Редагування параметрів

**Доступні стратегії:**
- **Orderbook Imbalance** - дисбаланс ордербуку
- **Volume Spike** - сплески обсягу
- **Mean Reversion** - повернення до середнього
- **Grid Trading** - сітковий трейдинг
- **DCA** - усереднення позиції

### Вкладка "Settings" (Налаштування)

#### Risk Management (Ризик-менеджмент)

| Параметр | Опис |
|----------|------|
| Max Position ($) | Максимальний розмір позиції |
| Max Daily Loss ($) | Денний ліміт втрат |
| Max Trades/Day | Максимум угод на день |
| Max Open Positions | Максимум відкритих позицій |
| Default SL (%) | Стоп-лосс за замовчуванням |
| Default TP (%) | Тейк-профіт за замовчуванням |
| Risk Per Trade (%) | Ризик на угоду |

#### Exchange (Біржа)

| Опція | Опис |
|-------|------|
| Exchange | Вибір біржі (Binance, Bybit, OKX, Kraken, KuCoin, Gate.io) |
| Use Testnet | Використовувати тестову мережу |

### Вкладка "Logs" (Логи)

- Перегляд системних логів в реальному часі
- Фільтрація за рівнем (INFO, WARNING, ERROR)
- Кнопка очищення логів

---

## Telegram бот

### Налаштування

1. **Створіть бота через @BotFather:**
   ```
   /newbot
   Назва бота: CryptoTrader
   Username: your_crypto_trader_bot
   ```

2. **Отримайте ваш Telegram ID:**
   - Напишіть @userinfobot
   - Скопіюйте ваш ID

3. **Налаштуйте .env:**
   ```bash
   TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
   TELEGRAM_CHAT_ID=your_chat_id
   TELEGRAM_ENABLED=true
   TELEGRAM_ALLOWED_USERS=your_user_id
   TELEGRAM_ADMIN_USERS=your_user_id
   ```

### Базові команди

| Команда | Опис |
|---------|------|
| `/start` | Запуск бота |
| `/help` | Список команд |
| `/status` | Статус бота |
| `/balance` | Баланс рахунку |
| `/positions` або `/pos` | Відкриті позиції |
| `/orders` | Відкриті ордери |
| `/pnl` | Прибуток/збиток |
| `/trades [N]` | Останні N угод |
| `/risk` | Метрики ризику |

### Команди керування (тільки адмін)

| Команда | Опис |
|---------|------|
| `/start_trading` | Запустити торгівлю |
| `/stop_trading` | Зупинити торгівлю |
| `/pause` | Призупинити |
| `/resume` | Продовжити |

### Торгові команди (тільки адмін)

| Команда | Приклад | Опис |
|---------|---------|------|
| `/buy` | `/buy BTCUSDT 0.001` | Купити (ринковий) |
| `/buy` | `/buy BTCUSDT 0.001 50000` | Купити (лімітний) |
| `/sell` | `/sell BTCUSDT 0.001` | Продати (ринковий) |
| `/long` | `/long BTCUSDT 0.001` | Alias для /buy |
| `/short` | `/short BTCUSDT 0.001` | Alias для /sell |
| `/close` | `/close BTCUSDT` | Закрити позицію |
| `/closeall` | `/closeall` | Закрити всі позиції |
| `/cancel` | `/cancel order_id` | Скасувати ордер |

### Команди SL/TP

| Команда | Приклад | Опис |
|---------|---------|------|
| `/sl` | `/sl BTCUSDT 49000` | Встановити стоп-лосс |
| `/tp` | `/tp BTCUSDT 52000` | Встановити тейк-профіт |
| `/sltp` | `/sltp BTCUSDT 49000 52000` | Встановити SL та TP |
| `/leverage` | `/leverage BTCUSDT 10` | Встановити плече |

### Команди статистики

| Команда | Опис |
|---------|------|
| `/stats` | Детальна статистика |
| `/daily` | Денна статистика |
| `/weekly` | Тижнева статистика |
| `/monthly` | Місячна статистика |

### Команди стратегій

| Команда | Опис |
|---------|------|
| `/strategies` | Список стратегій |
| `/enable [назва]` | Увімкнути стратегію |
| `/disable [назва]` | Вимкнути стратегію |

### Команди алертів

| Команда | Опис |
|---------|------|
| `/alerts` | Статус алертів |
| `/alerts on` | Увімкнути алерти |
| `/alerts off` | Вимкнути алерти |

### Швидкі дії

| Команда | Опис |
|---------|------|
| `/closeall` | Закрити всі позиції |
| `/panic` | Аварійна зупинка (закриває позиції + зупиняє бота) |
| `/exchange [назва]` | Переглянути/змінити біржу |

### Автоматичні сповіщення

Бот автоматично відправляє:

| Подія | Опис |
|-------|------|
| Position Opened | Відкриття позиції |
| Position Closed | Закриття позиції |
| Stop-Loss Hit | Спрацювання стоп-лосс |
| Take-Profit Hit | Спрацювання тейк-профіт |
| Daily Limit Reached | Досягнення денного ліміту |
| Trading Signal | Торговий сигнал |
| Error | Помилки системи |

---

## Торгові стратегії

### Orderbook Imbalance (Дисбаланс ордербуку)

Виявляє дисбаланс між покупцями та продавцями в ордербуці.

**Принцип роботи:**
1. Розраховує співвідношення bid/ask обсягів
2. Генерує сигнал при перевищенні порогу
3. LONG при домінуванні покупців, SHORT при домінуванні продавців

**Параметри:**
| Параметр | За замовч. | Опис |
|----------|------------|------|
| imbalance_threshold | 1.5 | Поріг дисбалансу |
| max_spread | 0.0005 | Макс. спред |
| levels | 5 | Рівні ордербуку |
| signal_cooldown | 10 | Кулдаун між сигналами (сек) |
| min_volume_usd | 5000 | Мін. обсяг |

**Найкраще для:** високоліквідні пари, волатильні ринки

### Volume Spike (Сплеск обсягу)

Детектує аномальні сплески торгового обсягу.

**Принцип роботи:**
1. Розраховує середній обсяг за період lookback
2. Спрацьовує при перевищенні множника
3. Напрямок визначається рухом ціни під час сплеску

**Параметри:**
| Параметр | За замовч. | Опис |
|----------|------------|------|
| volume_multiplier | 3.0 | Множник сплеску |
| lookback_seconds | 60 | Період усереднення |
| min_volume_usd | 10000 | Мін. обсяг |
| signal_cooldown | 15 | Кулдаун (сек) |

**Найкраще для:** новинні рухи, пробої

### Mean Reversion (Повернення до середнього)

Торгівля на основі відхилення від середнього значення.

**Принцип роботи:**
1. Розраховує Bollinger Bands
2. LONG при падінні нижче нижньої смуги
3. SHORT при зростанні вище верхньої смуги

**Параметри:**
| Параметр | За замовч. | Опис |
|----------|------------|------|
| lookback_period | 20 | Період SMA |
| std_dev_multiplier | 2.0 | Множник стандартного відхилення |
| entry_z_score | 2.0 | Z-score для входу |

**Найкраще для:** рейнджевий ринок, бічний тренд

### Grid Trading (Сітковий трейдинг)

Автоматизована торгівля в ціновому діапазоні.

**Принцип роботи:**
1. Розміщує ордери на різних цінових рівнях
2. Купує при зниженні, продає при зростанні
3. Прибуток від коливань всередині діапазону

**Параметри:**
| Параметр | За замовч. | Опис |
|----------|------------|------|
| grid_levels | 10 | Кількість рівнів |
| range_percent | 0.05 | Діапазон сітки (5%) |

**Найкраще для:** консолідація, боковик

### DCA (Dollar Cost Averaging)

Усереднення позиції протягом часу з підтримкою safety orders, trailing take profit та автоматичним розрахунком рівнів.

**Режими:**
- **time_based** - купівля через фіксовані інтервали
- **price_based** - купівля при падінні ціни
- **hybrid** - комбінація обох

**Напрямки:**
- **accumulate** - накопичення (long-term bullish)
- **distribute** - розподіл (taking profits)
- **bidirectional** - обидва напрямки

#### Базові параметри

| Параметр | За замовч. | Опис |
|----------|------------|------|
| mode | hybrid | Режим DCA |
| direction | accumulate | Напрямок (accumulate/distribute) |
| interval_minutes | 60 | Інтервал (для time_based) |
| dip_threshold | 0.02 | Поріг падіння (2%) |
| pump_threshold | 0.02 | Поріг росту (2%) |
| quantity_per_entry | 0.001 | Кількість на вхід |
| max_entries | 100 | Макс. кількість входів |
| total_budget | 1000 | Загальний бюджет |

#### Safety Orders (Ордери безпеки)

Safety orders автоматично докуповують позицію при падінні ціни, усереднюючи вхід.

| Параметр | За замовч. | Опис |
|----------|------------|------|
| safety_orders_enabled | false | Увімкнути safety orders |
| safety_orders_count | 5 | Кількість SO |
| safety_order_step_pct | 0.01 | Крок ціни (1%) |
| safety_order_step_scale | 1.5 | Множник кроку |
| safety_order_size_scale | 1.5 | Множник розміру |

**Приклад налаштування:**
```yaml
strategies:
  dca:
    enabled: true
    mode: hybrid
    safety_orders_enabled: true
    safety_orders_count: 5
    safety_order_step_pct: 0.02  # 2%
    safety_order_step_scale: 1.5
    safety_order_size_scale: 2.0
```

**Розрахунок рівнів SO (для entry @ $50,000):**
| Рівень | Відхилення | Ціна | Множник розміру |
|--------|------------|------|-----------------|
| SO #1 | 2% | $49,000 | 2x |
| SO #2 | 3% | $48,500 | 4x |
| SO #3 | 4.5% | $47,750 | 8x |
| SO #4 | 6.75% | $46,625 | 16x |
| SO #5 | 10.1% | $44,950 | 32x |

#### Take Profit / Stop Loss

| Параметр | За замовч. | Опис |
|----------|------------|------|
| take_profit_pct | 0.10 | Take profit (10%) |
| stop_loss_pct | 0.05 | Stop loss (5%) |

**Методи:**
```python
# Отримати ціни TP/SL
strategy.calculate_take_profit_price()  # Від середньої ціни
strategy.calculate_stop_loss_price()
strategy.get_tp_sl_prices()  # Повна інформація
```

#### Trailing Take Profit

Trailing TP слідує за ціною вгору та активує тейк при відкаті.

| Параметр | За замовч. | Опис |
|----------|------------|------|
| trailing_tp_enabled | false | Увімкнути trailing TP |
| trailing_tp_activation_pct | 0.05 | Активація при +5% |
| trailing_tp_deviation_pct | 0.02 | Тригер при -2% від хаю |

**Принцип роботи:**
```
1. Позиція відкрита @ $50,000
2. Ціна досягає $52,500 (+5%) → Trailing TP активовано
3. Ціна росте до $55,000 → Trigger оновлено: $53,900 (2% від хаю)
4. Ціна падає до $53,800 → Trailing TP спрацьовує!
5. Profit: +7.6% (замість +10% фіксованого)
```

**Приклад конфігурації:**
```yaml
strategies:
  dca:
    enabled: true
    mode: hybrid
    direction: accumulate

    # Safety orders
    safety_orders_enabled: true
    safety_orders_count: 5
    safety_order_step_pct: 0.02
    safety_order_size_scale: 2.0

    # Take profit з трейлінгом
    take_profit_pct: 0.10
    trailing_tp_enabled: true
    trailing_tp_activation_pct: 0.05
    trailing_tp_deviation_pct: 0.02

    # Stop loss
    stop_loss_pct: 0.05
```

#### API методи DCA

```python
from src.strategy.dca_strategy import DCAStrategy

strategy = DCAStrategy(config)

# Safety orders
safety_orders = strategy.calculate_safety_orders(entry_price)
triggered = strategy.check_safety_order_triggers(current_price, timestamp)
status = strategy.get_safety_orders_status()

# Take profit
tp_price = strategy.calculate_take_profit_price()
sl_price = strategy.calculate_stop_loss_price()
tp_sl_info = strategy.get_tp_sl_prices()

# Trailing TP
strategy.update_trailing_take_profit(current_price)
trailing_status = strategy.get_trailing_tp_status()

# Загальна статистика
stats = strategy.get_dca_stats()
unrealized = strategy.get_unrealized_pnl(current_price)
```

---

## Просунутий скальпінг

Система включає професійні інструменти скальпінгу, що базуються на концепціях:
- Класичного скальпінгу (торгівля за стаканом)
- Імпульсного скальпінгу (слідування за поводирями)
- Гібридного підходу (комбінація методів)

### Імпульсний скальпінг (Impulse Scalping)

Стратегія слідування за глобальними індексами-"поводирями".

**Принцип роботи:**
```
SPX рухається вгору (+0.2%) → Через кореляцію очікуємо рух BTC вгору
DXY падає (-0.1%) → Негативна кореляція → Очікуємо рух крипто вгору
```

**Індекси-поводирі:**
| Індекс | Кореляція з BTC | Затримка |
|--------|-----------------|----------|
| S&P 500 (SPX) | +0.7 | 500ms |
| Dollar Index (DXY) | -0.6 | 300ms |
| Gold (XAU) | +0.5 | 800ms |
| VIX | -0.65 | 200ms |

**Приклад сигналу:**
```
📊 IMPULSE SIGNAL

Source: SPX impulse +0.25%
Expected BTC impact: +0.175%
Confidence: 0.82

Direction: LONG
Entry: $65,000
Target: $65,115
Stop: $64,900

Confirmations:
✅ Orderbook imbalance: 1.8
✅ Tape flow: bullish
```

### Print Tape (Лента принтів)

Аналіз реальних угод у реальному часі.

**Що відстежується:**
- Агресивні покупці/продавці (market orders)
- Великі угоди (whale trades > $100k)
- Кластери угод (серії в одному напрямку)
- CVD (Cumulative Volume Delta)
- Швидкість потоку ордерів

**Метрики:**
```
📈 ORDER FLOW METRICS (10s)

Buy Volume:  $2,450,000 (65%)
Sell Volume: $1,350,000 (35%)
Delta: +$1,100,000 (+29%)

Whale Buys:  3 trades ($850k)
Whale Sells: 1 trade ($250k)

CVD: +$5,200,000 (bullish)
Flow Speed: 125 trades/sec
```

**Сигнали:**
- `aggressive_buying` - дельта > 20% (бичачий)
- `aggressive_selling` - дельта < -20% (ведмежий)
- `whale_cluster` - 3+ китів підряд
- `volume_spike` - 3x від середнього обсягу

### Advanced Order Book (Покращений стакан)

Розширений аналіз ордербуку з детекцією:

**1. Стіни ордерів (Walls)**
```
📊 BID WALL DETECTED

Price: $64,500
Volume: $2.5M
Distance: 0.3% below mid
Times seen: 5

💡 Interpretation:
Strong support level. May act as floor.
Consider LONG with SL below wall.
```

**2. Фронтраннінг**
```
🎯 FRONTRUN OPPORTUNITY

Large bid wall at $64,500 ($3M)
Distance: 0.4%

Strategy: LONG
Entry: $64,650
Target: $64,900 (wall bounce)
Stop: $64,400 (below wall)
```

**3. Детекція спуфінгу**
```
⚠️ POTENTIAL SPOOF DETECTED

Price: $65,000
Volume: $1.5M
Lifetime: 350ms (< 500ms threshold)

Action: IGNORE this level
```

**4. Виявлення ікебергів**
```
🧊 ICEBERG ORDER DETECTED

Price: $64,800
Visible: 5 BTC
Estimated hidden: ~50 BTC
Side: BUY

💡 Large buyer accumulating
```

### Cluster Analysis (Кластерний аналіз)

Об'ємний аналіз свічок для визначення зон накопичення/розподілу.

**Ключові концепції:**
- **POC (Point of Control)** - ціна з найбільшим обсягом
- **Value Area** - зона де пройшло 70% обсягу
- **Delta** - bid volume - ask volume
- **Imbalance** - дисбаланс на окремих рівнях

**Приклад кластера:**
```
📊 CLUSTER ANALYSIS (1m candle)

OHLC: 65000 → 65100 → 64950 → 65050

Price Levels:
65100 │ B: 12  A: 3   │ Δ: +9  │ BID IMBALANCE ✓
65050 │ B: 45  A: 48  │ Δ: -3  │ POC (max vol)
65000 │ B: 8   A: 35  │ Δ: -27 │ ASK IMBALANCE ✓
64950 │ B: 5   A: 18  │ Δ: -13 │

Total Delta: +15 (slight buying pressure)
Cluster Type: ACCUMULATION
```

**Сигнали кластерів:**
- `strong_delta` - дельта > 30%
- `stacked_bid_imbalance` - 3+ послідовних bid imbalances
- `stacked_ask_imbalance` - 3+ послідовних ask imbalances
- `initiative_buy/sell` - ініціативні покупки/продажі

### Гібридна стратегія скальпінгу

Комбінує всі методи для максимальної точності.

**Джерела сигналів:**
| Джерело | Вага | Опис |
|---------|------|------|
| Order Book | 35% | Стіни, дисбаланс, фронтран |
| Impulse | 25% | Індекси-поводирі |
| Tape | 25% | Потік ордерів, дельта |
| Cluster | 15% | POC, imbalances |

**Генерація сигналу:**
```
📊 HYBRID SIGNAL

Direction: LONG
Strength: 0.85
Confirmations: 3/4

Sources:
✅ Orderbook: Bid imbalance 1.6, wall at $64,500
✅ Impulse: SPX +0.2%, DXY -0.1%
✅ Tape: Delta +25%, 2 whale buys
⏳ Cluster: Neutral delta

Entry: $65,000
Stop Loss: $64,450 (below wall)
Take Profit: $65,350 (next resistance)
Risk/Reward: 1:2.3
```

**Налаштування:**
```yaml
hybrid_scalping:
  enabled: true
  min_confirmations: 2      # Мін. 2 з 4 джерел
  aggregation_mode: weighted # weighted, unanimous, majority

  weights:
    orderbook: 0.35
    impulse: 0.25
    tape: 0.25
    cluster: 0.15

  risk:
    default_sl_pct: 0.3     # 0.3%
    default_tp_pct: 0.5     # 0.5%
    dynamic_sl_tp: true     # Адаптивні SL/TP
```

---

## Управління ризиками

### Розмір позиції

Бот використовує формулу:
```
Розмір = (Баланс × Ризик на угоду) / Дистанція стопа
```

Приклад:
- Баланс: $1,000
- Ризик: 1%
- Стоп: 2%
- Розмір: ($1,000 × 0.01) / 0.02 = $500

### Захист від втрат

```yaml
risk:
  max_daily_loss_usd: 5      # Зупинка після $5 втрат
  max_drawdown_pct: 10       # Зупинка при 10% просадці
  max_trades_per_day: 20     # Ліміт угод
```

### Стоп-лосс та Тейк-профіт

**Типи стопів:**
- Фіксований стоп-лосс
- Trailing stop (плаваючий)
- Breakeven (переніс в беззбиток)

```yaml
risk:
  default_stop_loss_pct: 0.5   # 0.5% стоп
  default_take_profit_pct: 0.3  # 0.3% профіт
```

---

## Signal Provider

Система розсилки торгових сигналів підписникам з різними рівнями доступу.

### Огляд системи

Signal Provider дозволяє:
- Публікувати торгові сигнали для підписників
- Керувати рівнями підписки (FREE, BASIC, PREMIUM, VIP)
- Налаштовувати затримку доставки для різних рівнів
- Відстежувати ефективність сигналів
- Доставляти сигнали через різні канали

### Рівні підписки

| Рівень | Затримка | Канали | Опис |
|--------|----------|--------|------|
| FREE | 5 хв | Telegram | Базовий доступ |
| BASIC | 1 хв | Telegram, Email | Стандартна підписка |
| PREMIUM | 10 сек | Telegram, Email, Webhook | Преміум доступ |
| VIP | 0 сек | Всі канали + API | Миттєвий доступ |

### Типи сигналів

```python
# Типи торгових сигналів
SignalType.LONG           # Відкрити лонг
SignalType.SHORT          # Відкрити шорт
SignalType.CLOSE_LONG     # Закрити лонг
SignalType.CLOSE_SHORT    # Закрити шорт
SignalType.SCALE_IN       # Додати до позиції
SignalType.SCALE_OUT      # Частково закрити
```

### Формат сигналу

```
📊 TRADING SIGNAL

📈 Symbol: BTCUSDT
🔼 Type: LONG
💰 Entry: $65,000
🎯 Targets: $67,000, $68,500, $70,000
🛑 Stop Loss: $63,500

📊 Confidence: 85%
⚖️ Risk/Reward: 1:2.5

💡 Analysis: Пробій опору на зростаючому обсязі...
```

### Використання через API

```python
from src.signals.signal_provider import SignalProvider, TradingSignal

# Створення провайдера
provider = SignalProvider(
    provider_id="my_signals",
    provider_name="Pro Signals"
)

# Публікація сигналу
signal = TradingSignal(
    symbol="BTCUSDT",
    signal_type=SignalType.LONG,
    entry_price=65000.0,
    targets=[67000.0, 68500.0, 70000.0],
    stop_loss=63500.0,
    confidence=0.85,
    analysis="Пробій опору..."
)

await provider.publish_signal(signal)
```

### Статистика ефективності

```
📈 SIGNAL PERFORMANCE (30 days)

Total Signals: 45
Win Rate: 68.9%
Profit Factor: 2.15
Average P&L: +2.3%
Sharpe Ratio: 1.85

Best Trade: +8.5% (ETHUSDT)
Worst Trade: -2.1% (SOLUSDT)
```

---

## Smart DCA з AI/ML

Інтелектуальна стратегія усереднення з використанням машинного навчання.

### Що таке Smart DCA?

Smart DCA (Dollar Cost Averaging) - це розумна стратегія накопичення позиції, яка:
- Автоматично оцінює оптимальність моментів входу
- Використовує ML-модель для прогнозування
- Враховує Fear & Greed Index та інші метрики
- Адаптує розмір позиції до ринкових умов

### Режими роботи

| Режим | Опис |
|-------|------|
| `rule_based` | Класичні правила (RSI, BB, обсяг) |
| `ml_based` | Тільки ML-модель для рішень |
| `hybrid` | Комбінація правил та ML (рекомендовано) |

### Як працює оцінка входу

```
ENTRY EVALUATION SCORE

📊 Market Metrics:
  RSI (14): 28.5          → Score: +0.25 (Oversold)
  Bollinger: -1.8σ        → Score: +0.20 (Near lower band)
  Volume: 1.5x avg        → Score: +0.10 (Elevated)
  Fear & Greed: 22        → Score: +0.30 (Extreme Fear)

🤖 ML Model Prediction: 0.72 (Bullish)

📈 Final Score: 0.78 / 1.00
💡 Recommendation: STRONG BUY

Entry Size Multiplier: 1.5x
```

### Налаштування рівнів входу

```yaml
# Автоматичні рівні
smart_dca:
  entry_levels:
    - drop: 2%   →  size: 1.0x
    - drop: 4%   →  size: 1.5x
    - drop: 6%   →  size: 2.0x
    - drop: 10%  →  size: 3.0x
```

### ML Модель

Підтримувані моделі:
- **Random Forest** - швидка, інтерпретована
- **Gradient Boosting** - точніша, повільніша

Фічі для навчання:
- RSI, MACD, Bollinger Bands
- Обсяг відносно середнього
- Fear & Greed Index
- Funding Rate
- Open Interest зміни

### Приклад використання

```python
from src.strategy.smart_dca import SmartDCAStrategy, SmartDCAConfig

# Конфігурація
config = SmartDCAConfig(
    mode="hybrid",
    total_capital=1000.0,
    base_order_pct=0.1,
    use_ml=True
)

# Створення стратегії
strategy = SmartDCAStrategy(config)

# Оцінка точки входу
metrics = await strategy.get_market_metrics("BTCUSDT")
evaluation = strategy.evaluate_entry(metrics)

if evaluation.should_enter:
    print(f"Enter with {evaluation.size_multiplier}x size")
    print(f"Score: {evaluation.score}")
```

### Fear & Greed інтеграція

```
FEAR & GREED IMPACT

Index: 22 (Extreme Fear)
Classification: EXTREME_FEAR

Entry Multiplier: 1.5x
  → При екстремальному страху збільшуємо позицію

Historical Context:
  - Avg return after extreme fear: +15.3% (30 days)
  - Current level seen 5% of time
```

---

## Liquidation Heatmap

Аналіз рівнів ліквідації та визначення ключових цінових зон.

### Що таке Liquidation Heatmap?

Liquidation Heatmap показує:
- Рівні, де сконцентровані позиції з високим плечем
- Потенційні "магніти" для ціни
- Ризик каскадних ліквідацій
- Open Interest розподіл

### Чому це важливо?

```
Ліквідації створюють:
1. Магніти - ціна тяжіє до великих рівнів ліквідацій
2. Підтримку/опір - після ліквідації рівень очищається
3. Волатильність - каскад ліквідацій = різкий рух
4. Можливості - торгівля на ліквідаційних рухах
```

### Приклад аналізу

```
📊 LIQUIDATION HEATMAP - BTCUSDT

Current Price: $65,000

🔴 LONG LIQUIDATIONS (above):
  $63,000: $45M (5x-10x leverage)
  $62,000: $120M (10x-25x leverage)  ← Major cluster
  $60,000: $85M (25x-50x leverage)

🟢 SHORT LIQUIDATIONS (below):
  $67,000: $35M (5x-10x leverage)
  $68,500: $95M (10x-25x leverage)  ← Major cluster
  $70,000: $150M (25x-50x leverage) ← Magnet

⚠️ CASCADE RISK:
  Downside: HIGH (3 consecutive levels)
  Upside: MEDIUM (2 levels with gap)

💡 RECOMMENDATION:
  - Strong magnet at $70,000 (shorts)
  - Watch $62,000 for long squeeze
```

### Кластеризація рівнів

```python
from src.analytics.liquidation_heatmap import LiquidationHeatmapAnalyzer

analyzer = LiquidationHeatmapAnalyzer()

# Отримання даних
levels = await analyzer.fetch_liquidation_levels("BTCUSDT")

# Кластеризація
clusters = analyzer.cluster_levels(levels, tolerance_pct=0.5)

# Аналіз магнітів
magnets = analyzer.find_liquidation_magnets(
    clusters,
    current_price=65000,
    min_volume=50_000_000
)

for magnet in magnets:
    print(f"Magnet at ${magnet.price}: ${magnet.volume/1e6:.1f}M")
    print(f"  Distance: {magnet.distance_pct:.2f}%")
    print(f"  Gravity: {magnet.gravity_score:.2f}")
```

### Інтеграція в торгівлю

```yaml
# Торгові сигнали на основі ліквідацій
liquidation_trading:
  enabled: true

  # Торгівля до магніту
  magnet_trade:
    min_distance: 1.0%
    max_distance: 5.0%
    take_profit_at_magnet: true

  # Торгівля після каскаду
  post_cascade:
    wait_minutes: 5
    look_for_reversal: true
```

---

## News/Event Trading

Торгівля на основі новин, економічних подій та token unlocks.

### Компоненти системи

```
NEWS/EVENT TRADING SYSTEM

┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ Economic        │  │ Token           │  │ News            │
│ Calendar        │  │ Unlocks         │  │ Sentiment       │
└────────┬────────┘  └────────┬────────┘  └────────┬────────┘
         │                    │                    │
         └────────────────────┼────────────────────┘
                              │
                    ┌─────────▼─────────┐
                    │  Trading Engine   │
                    └───────────────────┘
```

### Економічний календар

| Подія | Вплив | Дія перед подією | Очікування після |
|-------|-------|------------------|------------------|
| FOMC | Високий | Закрити всі | 15 хв |
| CPI | Високий | Зменшити | 5 хв |
| NFP | Високий | Зменшити | 10 хв |
| GDP | Середній | Нічого | 5 хв |

```
⚠️ UPCOMING EVENT ALERT

📅 Event: FOMC Meeting
⏰ Time: 2024-12-18 19:00 UTC
🔴 Impact: HIGH

📊 Historical Impact:
  - Avg BTC move: ±3.2%
  - Avg volatility increase: 85%

💡 Recommended Actions:
  1. Close open positions
  2. Wait 15 min after announcement
  3. Watch for breakout direction
```

### Token Unlocks

```
🔓 TOKEN UNLOCK ALERT

Token: ARB (Arbitrum)
Date: 2024-12-16
Amount: 92.65M ARB
Value: ~$150M
% of Supply: 7.5%

📊 Historical Pattern:
  - Pre-unlock (7d): -12.3% avg
  - Post-unlock (7d): +5.2% avg

💡 Strategy:
  - Consider SHORT 7 days before
  - Close during unlock
  - Evaluate LONG after 24h
```

### Аналіз настрою новин

```python
from src.trading.news_event_trading import NewsEventTrader

trader = NewsEventTrader()

# Аналіз поточного настрою
sentiment = await trader.analyze_sentiment("BTCUSDT")

print(f"Sentiment Score: {sentiment.score}")  # -1 to +1
print(f"Classification: {sentiment.label}")   # bearish/neutral/bullish
print(f"Confidence: {sentiment.confidence}")  # 0 to 1
print(f"Key Headlines:")
for headline in sentiment.top_headlines:
    print(f"  - {headline}")
```

### Приклад торгівлі на новинах

```
📰 NEWS SENTIMENT SIGNAL

Symbol: BTCUSDT
Sentiment: 0.78 (BULLISH)
Confidence: 85%

Key Drivers:
  ✅ "Bitcoin ETF inflows reach $500M"
  ✅ "Major bank announces crypto custody"
  ⚠️ "Regulatory concerns in EU"

Signal: LONG
Entry: $65,000
Target: $67,500 (+3.8%)
Stop: $63,500 (-2.3%)
```

---

## Advanced Alerts

Розширена система алертів для моніторингу ринку.

### Типи алертів

| Категорія | Типи | Опис |
|-----------|------|------|
| Цінові | Above, Below, Cross, Change% | Рух ціни |
| Обсяг | Spike, OI Change | Аномалії обсягу |
| Кити | Transactions, Exchange Flows | Великі гравці |
| On-chain | NVT, MVRV, Funding | Метрики мережі |
| Технічні | RSI, MACD, BB | Індикатори |

### Створення алертів

```python
from src.alerts.alert_system import AdvancedAlertSystem

alerts = AdvancedAlertSystem()

# Ціновий алерт
await alerts.create_price_alert(
    symbol="BTCUSDT",
    condition="above",
    price=100000,
    message="BTC пробив $100k!"
)

# Алерт сплеску обсягу
await alerts.create_volume_spike_alert(
    symbol="BTCUSDT",
    multiplier=3.0,
    message="Аномальний обсяг на BTC!"
)

# Алерт китів
await alerts.create_whale_alert(
    min_value_usd=1000000,
    chains=["ethereum", "bitcoin"]
)
```

### Канали сповіщень

```
NOTIFICATION CHANNELS

┌──────────────┬──────────────┬──────────────┬──────────────┐
│   Telegram   │   Discord    │   Webhook    │    Email     │
├──────────────┼──────────────┼──────────────┼──────────────┤
│ ✅ Instant   │ ✅ Instant   │ ✅ Instant   │ ⏰ Delayed   │
│ ✅ Mobile    │ ✅ Rich      │ ✅ Custom    │ ✅ Detailed  │
│ ✅ Buttons   │ ✅ Embeds    │ ✅ API       │ ❌ Rate lim  │
└──────────────┴──────────────┴──────────────┴──────────────┘
```

### Приклад Telegram алерту

```
🚨 PRICE ALERT TRIGGERED

📊 Symbol: BTCUSDT
💰 Current Price: $100,150
📈 Condition: Price above $100,000

⏰ Triggered: 2024-12-15 14:30:00 UTC
🔄 Triggers: 1/10

[View Chart] [Set New Alert] [Dismiss]
```

### On-chain алерти

```
⛓️ ON-CHAIN ALERT

📊 Metric: Funding Rate
🔴 Status: EXTREME

Current: +0.035% (per 8h)
Annualized: +38.3%
Classification: Longs Overheated

📈 Historical Context:
  - 95th percentile (extreme)
  - Avg correction after: -8.3%

💡 Interpretation:
  Many traders are paying to hold longs.
  Consider reducing long exposure or
  looking for short opportunities.
```

### Розклад та cooldown

```yaml
# Налаштування розкладу
alerts:
  schedule:
    active_hours: "00:00-23:59"  # 24/7
    timezone: "UTC"

  cooldown:
    same_alert: 300      # 5 хв
    same_symbol: 60      # 1 хв
    global: 10           # 10 сек

  max_triggers:
    per_alert: 10
    reset_after: 24h
```

---

## Паперова торгівля

### Запуск

```bash
# Через командний рядок
python -m src.main --paper

# Або в конфігурації
trading:
  paper_trading: true
```

### Особливості

- Реальні ринкові дані
- Симульоване виконання з прослизанням (0.05%)
- Симуляція комісій (0.1%)
- Підтримка плеча
- Симуляція ліквідації

### Моніторинг

```bash
# Через Telegram
/status
/positions
/pnl

# Або через дашборд
http://localhost:8000
```

### Перехід на реальну торгівлю

Після успішного паперового тестування:

1. Перевірте стабільні позитивні результати протягом 1+ тижнів
2. Переконайтесь, що просадки в допустимих межах
3. Переконайтесь, що сповіщення працюють
4. Налаштуйте реальні API ключі
5. Почніть з зменшених розмірів позицій
6. Уважно моніторте перші кілька днів

---

## Бектестинг

### Запуск бектесту

```bash
# Базовий бектест
python scripts/backtest.py \
  --symbol BTCUSDT \
  --start 2024-01-01 \
  --end 2024-06-01 \
  --strategy momentum

# З кастомними параметрами
python scripts/backtest.py \
  --symbol BTCUSDT \
  --start 2024-01-01 \
  --end 2024-06-01 \
  --strategy orderbook_imbalance \
  --param threshold=2.0
```

### Інтерпретація результатів

```
═══════════════════════════════════════════════════════════════════
                         BACKTEST REPORT
═══════════════════════════════════════════════════════════════════

RETURNS
  Початковий баланс: $10,000.00
  Кінцевий баланс:   $12,450.00
  Загальна дохідність: 24.50%

TRADES
  Всього угод:       156
  Win Rate:          62.82%
  Profit Factor:     1.65

RISK METRICS
  Max Drawdown:      8.5%
  Sharpe Ratio:      1.85    ← Вище 1.0 - добре
  Sortino Ratio:     2.10    ← Вище 1.5 - добре
═══════════════════════════════════════════════════════════════════
```

**Ключові метрики:**

| Метрика | Мінімум | Добре | Відмінно |
|---------|---------|-------|----------|
| Win Rate | >50% | >55% | >60% |
| Profit Factor | >1.0 | >1.3 | >1.5 |
| Sharpe Ratio | >0.5 | >1.0 | >1.5 |
| Max Drawdown | <30% | <15% | <10% |

---

## Мульти-біржова торгівля

### Підтримувані біржі

| Біржа | Тестнет | Ф'ючерси | Пасфраза |
|-------|---------|----------|----------|
| Binance | Так | Так | Ні |
| Bybit | Так | Так | Ні |
| OKX | Так | Так | Так |
| Kraken | Так | Так | Ні |
| KuCoin | Так | Так | Так |
| Gate.io | Так | Так | Ні |

### Перемикання біржі

**Через веб-панель:**
Settings -> Exchange -> Вибрати біржу

**Через Telegram:**
```
/exchange binance
/exchange bybit
```

**Через .env:**
```bash
ENVIRONMENT=testnet
# Налаштуйте відповідні API ключі
```

---

## Сповіщення та алерти

### Типи сповіщень

| Тип | Опис |
|-----|------|
| Position Opened | Відкриття позиції з деталями |
| Position Closed | Закриття з P&L |
| Stop-Loss Hit | Спрацювання стопа |
| Take-Profit Hit | Спрацювання профіту |
| Daily Limit | Досягнення денного ліміту |
| Trading Signal | Новий сигнал стратегії |
| Error | Системні помилки |

### Налаштування

```yaml
notifications:
  telegram:
    trade_alerts: true      # Кожна угода
    signal_alerts: false    # Сигнали (шумно)
    daily_summary: true     # Денний звіт
    risk_warnings: true     # Попередження ризику
    error_alerts: true      # Помилки системи
```

### Увімкнення/вимкнення

```
/alerts on   # Увімкнути
/alerts off  # Вимкнути
```

---

## API Документація

### Базовий URL

```
http://localhost:8000
```

### Аутентифікація

```bash
# Логін
POST /api/auth/login
{
  "username": "admin",
  "password": "your_password"
}

# Відповідь
{
  "access_token": "eyJ...",
  "refresh_token": "eyJ...",
  "token_type": "bearer",
  "expires_in": 3600
}

# Використання токена
Authorization: Bearer eyJ...
```

### Основні ендпоінти

#### Статус та контроль

| Метод | Endpoint | Опис |
|-------|----------|------|
| GET | `/health` | Health check |
| GET | `/api/status` | Статус бота |
| POST | `/api/command` | Команди (start/stop/pause/resume) |

#### Торгівля

| Метод | Endpoint | Опис |
|-------|----------|------|
| POST | `/api/order` | Розмістити ордер |
| DELETE | `/api/order/{id}` | Скасувати ордер |
| GET | `/api/positions` | Отримати позиції |
| POST | `/api/position/close` | Закрити позицію |
| PUT | `/api/leverage` | Встановити плече |
| PUT | `/api/sl-tp` | Встановити SL/TP |

#### Стратегії

| Метод | Endpoint | Опис |
|-------|----------|------|
| GET | `/api/strategies` | Всі стратегії |
| PUT | `/api/strategies` | Оновити стратегію |
| POST | `/api/strategies/{name}/toggle` | Увімкнути/вимкнути |

#### Ризик та налаштування

| Метод | Endpoint | Опис |
|-------|----------|------|
| GET | `/api/risk` | Конфігурація ризику |
| PUT | `/api/risk` | Оновити ризик |
| GET | `/api/exchange` | Конфіг біржі |
| PUT | `/api/exchange` | Змінити біржу |

#### Дані

| Метод | Endpoint | Опис |
|-------|----------|------|
| GET | `/api/trades` | Історія угод |
| GET | `/api/signals` | Сигнали |
| GET | `/api/metrics` | Метрики |
| GET | `/api/equity` | Крива еквіті |
| GET | `/api/logs` | Логи |

### Приклади запитів

**Розмістити ринковий ордер:**
```bash
curl -X POST http://localhost:8000/api/order \
  -H "Content-Type: application/json" \
  -d '{
    "symbol": "BTCUSDT",
    "side": "BUY",
    "quantity": 0.001,
    "order_type": "MARKET"
  }'
```

**Закрити позицію:**
```bash
curl -X POST http://localhost:8000/api/position/close \
  -H "Content-Type: application/json" \
  -d '{"symbol": "BTCUSDT"}'
```

**Оновити ризик:**
```bash
curl -X PUT http://localhost:8000/api/risk \
  -H "Content-Type: application/json" \
  -d '{
    "max_position_usd": 50,
    "max_daily_loss_usd": 10
  }'
```

---

## FAQ

### Загальні питання

**П: Скільки грошей потрібно для початку?**
В: Рекомендуємо мінімум $500-1000 для можливості правильного управління ризиками.

**П: Яку біржу обрати?**
В: Binance Futures - найбільша ліквідність та найнижчі комісії. Почніть там.

**П: Чи можна використовувати кілька стратегій одночасно?**
В: Так, бот агрегує сигнали від усіх увімкнених стратегій.

### Технічні питання

**П: Чому бот не відкриває позиції?**
В: Перевірте:
- Ліміти ризику (денний ліміт, макс. позицій)
- Параметри стратегій (пороги можуть бути занадто суворими)
- Ринкові умови (низька волатильність)
- Логи на помилки

**П: Як скинути паперову торгівлю?**
В: Видаліть базу даних або:
```bash
python -m src.main --paper --reset
```

**П: Чи можна запускати бота 24/7?**
В: Так, бот розроблений для безперервної роботи. Використовуйте Docker для продакшн-деплою.

### Стратегії

**П: Яка стратегія найприбутковіша?**
В: Залежить від ринкових умов. Бектестуйте всі стратегії на свіжих даних.

**П: Як створити власну стратегію?**
В: Наслідуйте клас `BaseStrategy`:
```python
from src.strategy.base import BaseStrategy

class MyStrategy(BaseStrategy):
    def analyze(self, data):
        # Ваша логіка
        return signal
```

### Безпека

**П: Як захистити API ключі?**
В:
- Використовуйте IP whitelist на біржі
- Вимкніть можливість виведення коштів
- Зберігайте ключі в .env (не комітьте!)
- Використовуйте окремі ключі для тестування

**П: Що робити при підозрі на компрометацію?**
В:
1. Негайно деактивуйте API ключі на біржі
2. Створіть нові ключі
3. Перевірте логі на підозрілу активність

---

## Підтримка

- **GitHub Issues:** [Створити issue](https://github.com/your-repo/issues)
- **Telegram:** @your_support_bot
- **Документація:** [docs/](./docs/)

---

*Останнє оновлення: Грудень 2025*
